  cowrie:
    build:
      context: cowrie
    ports:
    - "{{cowrie_ssh}}:2222"
    - "{{cowrie_telnet}}:2323"
    volumes:
    - {{cowrie_path}}/logs:{{cowrie_path}}/logs
    - {{cowrie_path}}/download:{{cowrie_path}}/download
    restart: always
