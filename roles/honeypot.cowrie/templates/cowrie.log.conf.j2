input {
  file {
    path  => [ "{{cowrie_path}}/logs/cowrie.json" ]
    type  => "cowrie"
    codec => "json"
  }
}

filter {
  if [type] == "cowrie" {
    date { match => [ "timestamp", "ISO8601" ] }
  }
}
