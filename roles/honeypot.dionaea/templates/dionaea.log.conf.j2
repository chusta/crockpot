input {
  file {
    path  => [ "{{dionaea_path}}/logs/dionaea.json" ]
    codec => "json"
    type  => "dionaea"
  }
}

filter {
  if [type] == "dionaea" {
    date { match => [ "timestamp", "ISO8601" ] }
  }
}
